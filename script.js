function sortedString(str) {
        let splitString = str.split('');
        console.log(splitString)
        let sortedArray = splitString.sort();
        return sortedArray;
        }
        
        console.log(sortedString("Akademia108"))